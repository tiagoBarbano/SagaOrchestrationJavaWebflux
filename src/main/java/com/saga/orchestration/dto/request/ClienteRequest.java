package com.saga.orchestration.dto.request;

public record ClienteRequest(String nome, String email, String telefone) {
    // No additional methods or fields needed; this is a simple record for client data

}
